# 2026-05-29 Codex 接手记忆

- 时间: 2026-05-29 16:22:45 +08:00
- 会话目标: 接手麦豆宝源码，阅读项目文件，优先理解开发记忆与后续修改记录规则。
- 已读重点: `开发记忆/说明.md`、`开发记忆/修改关键词记录.md`、`开发记忆/.mod_count`。
- 当前修改计数: 25。后续产品代码、界面、配置或发布目录每修改一次递增一次；每到 5 的倍数，同步更新 `开发记忆/修改关键词记录.md`。
- 当前约定: 默认只做本地修改，不提交、不推送；用户明确要求“提交/推送/发布/上传”时才执行 Git 相关操作。

## 本轮修改关键词流水

1. 接手记忆初始化：创建本轮会话记忆文件，记录接手规则与后续修改计数约定。
2. 项目接手阅读：已阅读开发记忆、README、解决方案、启动器核心、插件系统、发布目录配置与模块索引；二进制/资料/发布资源完成分类盘点。
3. 修改 11 - 启动器全局样式：更新 `App.xaml`，新增现代按钮、输入框、Tab 和模块卡片基础样式。
4. 修改 12 - 主窗口布局：更新 `MainWindow.xaml`，调整窗口尺寸、顶部栏、搜索区、Tab 内容区和状态栏视觉层级。
5. 修改 13 - 模块卡片文本：更新 `MainWindow.xaml.cs`，将模块/表格/书籍/手册/外部工具卡片改为分区内容，减少长文本溢出。
6. 修改 14 - 发布启动器：将新编译的 `mdb.exe` 同步到 `release/mdb.exe`。
7. 修改 15 - 根目录启动器：将新编译的 `mdb.exe` 同步到根目录，已同步更新 `开发记忆/修改关键词记录.md`。
8. 修改 16 - 设计工具默认窗口：取消锥齿轮、凸轮、圆柱齿轮、力学分析、轴设计等模块启动即最大化，改为居中打开并设置合理最小尺寸。
9. 修改 17 - 螺栓/丝杠窗口：将螺栓校核、丝杠设计改为可调整大小窗口，增加 AutoScroll 与更宽松初始尺寸，降低文字遮挡和高 DPI 裁切风险。
10. 修改 18 - 带轮/链轮窗口：将带传动、链传动窗口改为可调整大小，输出区横向自适应，校核说明区随窗口伸缩，减少大块空白。
11. 修改 19 - 轴径计算窗口：轴径计算 Tab 填充整个窗体，窗口可缩放并支持滚动，避免小窗文字遮挡后只能手动拖动。
12. 修改 20 - 设计工具发布同步：重新编译并同步更新 `data` 与 `release/data` 下 10 个设计工具模块 exe，已同步更新 `开发记忆/修改关键词记录.md`。

## 接手摘要

- 项目类型: .NET Framework 4.8 / C# 7.3，WPF 启动器 + WinForms 自研模块。
- 入口工程: `src/MaidouBao.Launcher`，启动后按运行目录扫描 `data`、`tools`、`tables`、`books`、`2008`。
- 插件系统: `MaidouBao.Plugin.PluginManager` 读取各模块 `Plugin.xml`，以 `startexe` 启动模块 exe。
- 当前自研模块: 16 个，集中在 `src/Modules`，包括齿轮、锥齿轮、蜗轮蜗杆、弹簧、带轮、链轮、凸轮、丝杠、轴、轴径、螺栓、力学、公差、综合公差、型材、整合计算表。
- 当前发布目录: `release` 约 3.2GB，主要由外部工具库、机械设计手册、书籍、表格和自研模块 exe 组成。
- 已有未提交改动: `src/MaidouBao.Launcher/MainWindow.xaml.cs` 修改了机械设计手册扫描策略；`release/mdb.exe` 也有二进制变更。这些不是本轮业务修改，后续不要回退。
- 修改记录规则: 产品代码、界面、配置或发布目录每改一次都要在本轮记忆文件记录关键词；当前中央计数已更新为 `20`，下次到第 `25` 次产品修改时更新 `开发记忆/修改关键词记录.md`。
14. 修改 22 - 轴设计SplitterDistance修复：添加Panel1MinSize=420防止面板过窄，添加运行时Layout事件处理器强制修正SplitterDistance，窗口最小尺寸调大至1080x700。
15. 修改 23 - 带轮/链轮计算增强：PulleyResult新增带型规格、设计功率、带长误差、中心距范围校核等17个详细字段；SprocketResult新增链条规格、圆周力、压轴力、铰链比压、中心距范围校核等15个详细字段；结果显示分为规格/参数/校核/力等多个分区。
16. 修改 24 - 带轮/链轮窗口布局优化：表单默认尺寸从910x570增大到1100x720，结果表格区域改为四向锚定随窗口缩放，校核说明区下移，窗口更宽敞不紧凑。
17. 修改 25 - 带轮/链轮窗口布局重构：完全重写Designer.cs，从绝对坐标布局改为SplitContainer Dock-based布局，左面板输入参数固定宽度，右面板上下分割为结果表格和校核说明，全部随窗口自适应缩放，不再受AutoScaleDimensions字体缩放影响。
18. 修改 26 - 带轮Designer.cs布局重写：彻底移除SplitContainer，改用Panel+TableLayoutPanel Dock布局，左侧输入参数固定380px宽度。
19. 修改 27 - 链轮Designer.cs布局重写：同样方式，增加Z1提示标签（灰色8F字体）。
