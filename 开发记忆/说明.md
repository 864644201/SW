# 开发记忆说明

这个目录用于记录第二版开发过程中的修改记忆。

规则：

1. 每次代码、界面、配置或发布目录发生修改，都记录一次。
2. 每次记录以“时间 + 关键词 + 简要说明”的形式追加。
3. 关键词尽量简短，方便后续检索，例如：`表格工具`、`菜单新增`、`参数输入`、`导出功能`。
4. 这里默认记录的是本地开发过程，不代表已经提交到仓库。

当前约定：

1. 现在处于第二版开发阶段。
2. 默认只做本地修改，不提交、不推送。
3. 只有你明确要求时，才会提交或发布到仓库。
